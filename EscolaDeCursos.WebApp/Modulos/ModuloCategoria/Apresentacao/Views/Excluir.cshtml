@model CategoriaExcluirViewModel
@using EscolaDeCursos.WebApp.Modulos.ModuloCategoria.Apresentacao.ViewModels
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Excluir Categoria";
}

<div class="card shadow-sm border-danger-subtle" style="max-width: 640px;">
    <div class="card-body p-4">
        <p>Tem certeza que deseja excluir a categoria <strong>@Model.Titulo</strong>?</p>
        <p class="text-secondary small">Categorias com cursos vinculados não podem ser excluídas.</p>

        <form asp-action="Excluir" method="post">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Titulo" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Confirmar exclusão</button>
                <a asp-action="Listar" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>
