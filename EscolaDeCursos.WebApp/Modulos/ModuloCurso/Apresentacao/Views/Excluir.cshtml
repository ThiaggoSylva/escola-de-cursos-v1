@model CursoExcluirViewModel
@using EscolaDeCursos.WebApp.Modulos.ModuloCurso.Apresentacao.ViewModels
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Excluir Curso";
}

<div class="card shadow-sm border-danger-subtle" style="max-width: 640px;">
    <div class="card-body p-4">
        <p>Tem certeza que deseja excluir o curso <strong>@Model.Titulo</strong>?</p>
        <p class="text-secondary small">Cursos com turmas vinculadas não podem ser excluídos.</p>

        <form asp-action="Excluir" method="post">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Titulo" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Confirmar exclusão</button>
                <a asp-action="Listar" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>
